@{
    var controller = (String)ViewContext.RouteData.Values["Controller"];
    Layout =
        Request.IsAuthenticated
            ? "~/Views/Shared/_" + controller + "Authenticated.cshtml"
            : "~/Views/Shared/_" + controller + "Unauthenticated.cshtml";
}